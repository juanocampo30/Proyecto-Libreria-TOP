debemos eliminar el libro del DOM y del array. La parte facil es el DOM esa te dejare pensarlo como hacerlo y de preferencia hazlo usando el randon id de crypto para que no se desperdicie. el del array tambien es facil ya tienes las variables indice y array ahora solo usalas con splice,intentar tambien pensar que al crear la tarjeta tiene un index en el array, al eliminarla ese index desaparaece pero las demas tarjetas que creamos ya tienen un index al crearlas,que pasa con ese index ? se refresca ? o como ? no sera mejor que eliminar recorra de nuevo el array, devolviendo su index y ahora si lo borremos? osea usar una funcion como for each que recorra pero si devuelva y luego borrar, en este casi indec y array ya no nos sirven mejor pensar bien esto (ya borre index y array de a funcion , no estan mas) ESTO SE TENDRIA QUE HACER DENTRO DE LA FUNCION PUES ES FOR EACH QUE PASARA PRIMERO EL ARRAY OSE A TENDREMOS OTRO METODO DE ARRAY DENTRO DE FOR EACH- LA CLAVE AQUI SERA ENTONCES LA PROPIEDAD ARRAY DE FOR EACH