Cada libro sera un objeto, cada objeto (libro) va a contener 4 parametros (titulo, autor, paginas, leido) osea algo asi {titulo:, autor:, paginas, leido:} como cada objeto estara dentro del array general de libros, vamos a tener que acceder usando arr[0].titulo por ejemplo.
No necesitamos buscar una forma de que cada libro se guarde dentro de una variable, solamente necesitamos una forma de hacer que al presionar el boton se llame al constructor Book, cree el libro con los parametros del formulario y meta ese objeto creado dentro del array.
Vamos a crear el formulario con html y CSS y vamos a buscar la forma de obtener la informacion entrada en los campos de texto. Para esto vamos a regresar al ejemplo de la calculadora para refrescarnos la memoria sobre los eventListeners y el DOM manipiuleichon. (refresco de memoria sobre formularios y DOM manipiuleichon)
Consigamos estos tres primeros pasos y despues nos preocupamos por como hacemos que los objetos creados y puestos dentro del array se muestren en pantalla.